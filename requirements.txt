# ========================================
# Tokopedia Scraper API Dependencies
# ========================================

# Core web framework and utilities
flask==2.3.3              # Web framework for API endpoints
flask-cors==4.0.0         # Cross-origin resource sharing support
werkzeug==2.3.7           # WSGI utility library (Flask dependency)

# Professional REST API framework
flask-restx==1.3.0        # REST API framework with auto-generated Swagger docs

# HTTP client and data processing
requests>=2.31.0          # HTTP library for GraphQL requests

# Caching and data storage
redis==5.0.1              # Redis client for caching and data persistence

# Fuzzy search capabilities
fuzzywuzzy==0.18.0        # String similarity matching for search
python-levenshtein==0.25.1 # C extension for improved fuzzy matching performance

# HTML5 Parsing Libraries (Enhanced)
beautifulsoup4==4.12.2    # Enhanced HTML parsing with excellent community support
lxml==5.1.0               # Fast XML/HTML parser for optimal performance with BeautifulSoup
html5lib==1.1             # Extremely lenient HTML5 parser for edge cases
selectolax==0.3.1         # Keep as fallback option for speed-critical scenarios

# ========================================
# Python Standard Library (Built-in)
# ========================================
# datetime, json, logging, os, ssl, sys, time
# functools, typing - All included in Python 3.10+